/**
 * AUTO-FILE-COMMENT: postcss.config.mjs
 * Purpose: Explains the role of this module and documents its functions.
 * Notes: Comments are documentation-only and do not change runtime behavior.
 */
const config = {
  plugins: {
    "@tailwindcss/postcss": {},
  },
};

export default config;
