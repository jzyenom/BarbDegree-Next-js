import { redirect } from "next/navigation";

/**
 * AUTO-FUNCTION-COMMENT: DashboardMePage
 * Purpose: Handles dashboard me page.
 * Line-by-line:
 * 1. Executes `redirect("/dashboard");`.
 */
export default function DashboardMePage() {
  redirect("/dashboard");
}
